const API_BASE = "/api/v1";

export const API_CONFIG = {
  // 头像相关
  AVATAR: {
    BASE: `${API_BASE}/chat/character/avatar`,
    DEFAULT: "default.png",
  },

  // 其他API端点...
  AUTH: `${API_BASE}/auth`,
};
